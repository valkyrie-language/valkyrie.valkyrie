namespace package::lexer;

class Token {
    constructor(type, value, line: u32, column: u32) {
        self.type = type;
        self.value = value;
        self.line = line;
        self.column = column;
    }
}
