namespace package::compiler;

using package::parser::Node;

class NamespaceManager {
    constructor(source) {
        self.namespaces = {};
        self.usings = {};
        self.current_namespace = "";
        self.current_file = "";
    }
}
