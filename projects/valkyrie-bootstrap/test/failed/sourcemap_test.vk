namespace sourcemap_test;

# Test sourcemap configuration in CompilerOptions
class SourceMapTest {
    constructor() {
        self.name = "SourceMapTest";
    }
    
    micro test_source_map_enabled() {
        # This test verifies that source_map option is available
        let options = CompilerOptions("js", false, false, "test");
        return options.source_map;
    }
    
    micro test_basic_functionality() {
        # Basic test to ensure the class works
        return true;
    }
}

# Simple function to test sourcemap tracking
micro calculate_sum(a, b) {
    let result = a + b;
    return result;
}

# Test class instantiation
let test_instance = SourceMapTest();
let sum_result = calculate_sum(10, 20);