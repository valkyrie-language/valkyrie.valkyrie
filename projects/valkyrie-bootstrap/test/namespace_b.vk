namespace app::main;

using math::utils::add;
using math::utils::Calculator;

micro main() {
    let result = add(5, 3);
    let calc = new Calculator();
    let result2 = calc.calculate(10, 20);
    return result + result2;
}